## Docker for reporting-init

```
$ mkdir connector_api_calls kib_dashboards
$ cp ../../deployment/v3/reporting/ref_connector_api_calls/*.api connector_api_calls
$ cp ../../deployment/v3/reporting/ref_kib_dashboards/* kib_dashboards
$ docker build .
```
