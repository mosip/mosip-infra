apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: main-ingress
  namespace: default

spec:
  rules:
  - http:
      paths:
      - path: /config
        backend:
          serviceName: kernel-config-server 
          servicePort: 80 

