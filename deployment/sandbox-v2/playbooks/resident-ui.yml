- hosts: console
  vars:
    kube_config: '{{clusters.mz.kube_config}}'
    install_name: 'resident-ui'
    helm_chart: '{{charts_root}}/resident-ui'
    is_template: true
    helm_namespace: 'default'
    helm_values: '{{charts_root}}/resident-ui/values.template.j2'
    helm_strings: ''
  roles:
    - {role:  helm}