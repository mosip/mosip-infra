---
- hosts: "{{scancluster}}"
  tasks:
    - name: install openscap scanner
      package:
        name: "{{ item }}"
        state: absent
      with_items:
        - openscap-scanner
        - scap-security-guide