# This end point connects to git http container
kind: Endpoints
apiVersion: v1
metadata:
  name: gitrepo
subsets:
  - addresses:
    - ip: "{{hostvars['console']['ansible_host']}}"
    ports:
      - port: {{config_gitrepo_port}} 
