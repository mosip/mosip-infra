apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.misp.serviceName }} 
  namespace: {{ .Release.Namespace }} 
spec:
  ports:
  - port: {{ .Values.services.misp.ports.servicePort }}
    targetPort: {{ .Values.services.misp.ports.containerPort }}
  selector:
    app: {{ .Values.services.misp.serviceName }} 

