apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.partner1.serviceName }} 
  namespace: {{ .Release.Namespace }} 
spec:
  ports:
  - port: {{ .Values.services.partner1.servicePort }}
    targetPort: {{ .Values.services.partner1.containerPort }}
  selector:
    app: {{ .Values.services.partner1.serviceName }} 

