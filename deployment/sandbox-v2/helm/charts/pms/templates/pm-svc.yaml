apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.pm.serviceName }} 
  namespace: {{ .Release.Namespace }} 
spec:
  ports:
  - port: {{ .Values.services.pm.ports.servicePort }}
    targetPort: {{ .Values.services.pm.ports.containerPort }}
  selector:
    app: {{ .Values.services.pm.serviceName }} 

