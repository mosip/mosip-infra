apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.credentialservice.serviceName }} 
  namespace: {{ .Release.Namespace }} 
  labels:
    app: {{ .Values.services.credentialservice.serviceName }}
    component: {{ .Values.component }} 
    
spec:
  replicas: {{ .Values.services.credentialservice.podconfig.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.credentialservice.serviceName }} 
  minReadySeconds: {{ .Values.services.credentialservice.podconfig.minReadySeconds }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.services.credentialservice.podconfig.maxUnavailable }}
      maxSurge: {{ .Values.services.credentialservice.podconfig.maxSurge }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.credentialservice.serviceName }} 
        component: {{ .Values.component }} 
        exportLogs: {{ .Values.exportLogs }}
    spec:
      {{- include "dockerHubSecret" .  | indent 6}}
      containers:
      - name: {{ .Values.services.credentialservice.serviceName }} 
        image: {{ .Values.services.credentialservice.dockerImage }}
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        {{ $resources := .Values.services.credentialservice.podconfig.resources }}
        {{- include "resourcesDefinition" (dict "res" $resources) | indent 8 }}
        env:
        - name: active_profile_env
          value: {{ .Values.activeProfileEnv }} 
        - name: spring_config_label_env
          value: {{ .Values.springConfigLabel }} 
        - name: spring_config_url_env
          value: {{ .Values.configServerUrl }} 
        - name: artifactory_url_env
          value: {{ .Values.artifactoryUrl }} 
        - name: is_glowroot_env
          value: {{ .Values.isGlowroot }}
        - name: iam_adapter_url_env
          value: {{ .Values.iamAdapterUrl }}
        ports:
        - containerPort: {{ .Values.services.credentialservice.containerPort }}

        readinessProbe:
          httpGet:
            path: {{ .Values.services.credentialservice.healthUrl }} 
            port: {{ .Values.services.credentialservice.containerPort }} 
          initialDelaySeconds: {{ .Values.probe.initialDelay }} 
          periodSeconds: {{ .Values.probe.period }} 
 #       livenessProbe:
 #         httpGet:
 #           path: {{ .Values.services.credentialservice.healthUrl }} 
 #           port: {{ .Values.services.credentialservice.containerPort }} 
 #         initialDelaySeconds: {{ .Values.probe.initialDelay }} 
 #         periodSeconds: {{ .Values.probe.period }}
 #
