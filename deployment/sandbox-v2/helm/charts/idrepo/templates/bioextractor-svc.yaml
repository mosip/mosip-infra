apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.bioextractor.serviceName }} 
  namespace: {{ .Release.Namespace }} 
spec:
  ports:
  - port: {{ .Values.services.bioextractor.ports.servicePort }}
    targetPort: {{ .Values.services.bioextractor.ports.containerPort }}
  selector:
    app: {{ .Values.services.bioextractor.serviceName }} 

