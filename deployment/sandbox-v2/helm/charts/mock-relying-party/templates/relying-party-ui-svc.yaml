---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.relyingPartyUi.serviceName }}
  namespace: default
spec:
  selector:
    app: {{ .Values.services.relyingPartyUi.serviceName }}
  ports:
    - port: 80
      targetPort: {{ .Values.services.relyingPartyUi.ports.containerPort }}
