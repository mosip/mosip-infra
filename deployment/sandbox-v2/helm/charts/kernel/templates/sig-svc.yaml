apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.sig.serviceName }} 
  namespace: {{ .Values.namespace }} 
spec:
  ports:
  - port: {{ .Values.services.sig.servicePort }}
    targetPort: {{ .Values.services.sig.containerPort }}
  selector:
    app: {{ .Values.services.sig.serviceName }} 

