apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.applicant.serviceName }} 
  namespace: {{ .Values.namespace }} 
spec:
  ports:
  - port: {{ .Values.services.applicant.servicePort }}
    targetPort: {{ .Values.services.applicant.containerPort }}
  selector:
    app: {{ .Values.services.applicant.serviceName }} 

