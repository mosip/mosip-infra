apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.uin.serviceName }} 
  namespace: {{ .Values.namespace }} 
spec:
  ports:
  - port: {{ .Values.services.uin.servicePort }}
    targetPort: {{ .Values.services.uin.containerPort }}
  selector:
    app: {{ .Values.services.uin.serviceName }} 

