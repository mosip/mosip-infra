apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.crypto.serviceName }} 
  namespace: {{ .Values.namespace }} 
spec:
  ports:
  - port: {{ .Values.services.crypto.servicePort }}
    targetPort: {{ .Values.services.crypto.containerPort }}
  selector:
    app: {{ .Values.services.crypto.serviceName }} 

