apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.email.serviceName }} 
  namespace: {{ .Values.namespace }} 
spec:
  ports:
  - port: {{ .Values.services.email.servicePort }}
    targetPort: {{ .Values.services.email.containerPort }}
  selector:
    app: {{ .Values.services.email.serviceName }} 

