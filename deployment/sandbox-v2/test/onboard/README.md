* **device_spec.csv**: the expectation is the secondary language row appears after the primary language.
* **device_type.csv**:  The device code much match codes in `mosip_regdevice/reg_device_type`.
